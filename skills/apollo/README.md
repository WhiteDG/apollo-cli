# apollo skill

在支持 skills 的 AI 工具里查看、修改、删除、发布 Apollo 配置中心的配置。

## 安装

- 前提：运行环境有 Node.js >= 21。
- 可以用 skills CLI 安装：`npx skills add https://github.com/WhiteDG/apollo-cli --skill apollo`。
- 首次使用需要先准备 profile 与凭据（Portal 账号密码）——见 `SKILL.md` 的「可用性检查」一节。
